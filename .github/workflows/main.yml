name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checks out the repository so the runner can access the files.
      - uses: actions/checkout@v4

      # Simple build placeholder.
      - name: Run a one-line script
        run: echo "Write your CI workflow here!"

  test:
    runs-on: ubuntu-latest
    steps:
      # Checks out the repository so the runner can access the files.
      - uses: actions/checkout@v4

      # Install dependencies before testing.
      - name: Install dependencies
        run: npm install

      # Run the actual tests.
      - name: Run tests
        run: npm test
